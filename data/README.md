# Data

`patch-N.MPQ/ is required to be present in the Client's Data folder (`./Data/patch-N.MPQ`) to avoid repeated crashes when
logging into Naxx40. This patch contains `MapDifficulty.dbc` which adds 10M HC difficulties for both Onyxia and Naxxramas maps.
